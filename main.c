#include <stdio.h>
#include <math.h>

unsigned long int fibo(int n){

    return (n==0?0 : n==1?1 : fibo(n-1)+fibo(n-2));
}

int extract (unsigned long int q){
    while(n>9)
    {
        n=n/10;
    }
    return n;
}

unsigned long int fibo2(int n){
float phi= 1/2 +sqrt(5)/2;
return (unsigned long int) round((phi,n)/sqt(5));
}
int main(){


}